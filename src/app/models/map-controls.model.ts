// Describes the various types of map controls that can be activated
export enum mapControl {
  geolocate = 'geolocate',
  home = 'home',
  nav = 'nav',
  pointer = 'pointer',
  geocoder = 'geocoder',
  featureClick = 'featureClick'
}
